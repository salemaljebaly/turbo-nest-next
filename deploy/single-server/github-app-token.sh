#!/usr/bin/env bash
set -Eeuo pipefail

echo "GitHub App token generation requires APP_ID, INSTALLATION_ID, and PRIVATE_KEY."
echo "Install a real implementation for your server or replace this script with gh auth."
exit 1
